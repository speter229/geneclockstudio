import os

# Base API URL
API_URL  = "https://127.0.0.1:8500"  # Use localhost for local testing and deployment

# Define the project root directory
PROJECT_ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), "../"))